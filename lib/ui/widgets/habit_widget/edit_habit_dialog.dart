import 'package:flutter/material.dart';

class EditHabitDialog extends StatelessWidget {
  const EditHabitDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return AlertDialog(
      backgroundColor: Colors.grey,
      content: Container(
        height: 160,
      ),
    );
  }
}
